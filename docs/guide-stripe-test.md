# Stripe Test-Mode Integration Guide

Target: `api.stripe.com` | Scheme: `bearer` | Capability: `payments_read`

Bootstrap policy: use `stripe-test-policy.json` from
`council/r45-5-rest-auth-proofs/` as a template.

[Full guide TBD]
