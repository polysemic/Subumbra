# Custom Header-Auth Integration Guide

Use `auth.scheme: "header"` with `auth.header_name: "<your-header>"` in the
policy document.

Proof target: `api.anthropic.com` with `x-api-key`.

[Full guide TBD]
