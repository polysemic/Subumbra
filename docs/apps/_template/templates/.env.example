# Optional stub for app rounds that prove an env-file-based install path.
# Copy and replace placeholders only when the approved round plan requires an
# env template for the app under test.

APP_IMAGE=<image>
APP_PORT=<host-port>
APP_BASE_URL=<url>
