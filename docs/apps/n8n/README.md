# n8n

This folder currently contains workflow assets only.

Current contents:

- workflow JSON files for direct and Subumbra-mediated testing

Scope note:

- install and takeover operator docs are **not** part of the proved
  documentation set for `n8n` yet
- fuller `n8n` operator documentation belongs to a future `n8n` validation round
