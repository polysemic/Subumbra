{
  "name": "subumbra-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Subumbra CF Worker — decrypts API keys inside CF infrastructure",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "4.90.1"
  }
}