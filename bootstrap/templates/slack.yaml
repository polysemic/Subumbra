adapter_notes: Slack Web API; bot or user token as bearer.
allow:
  content_types:
  - application/json
  max_body_bytes: 1048576
  methods:
  - GET
  - POST
  path_prefixes:
  - /api
auth:
  scheme: bearer
capability_class: custom_rest
protocol: http_rest
target:
  host: api.slack.com
