#!/usr/bin/env python3
"""Shared bootstrap constants and helpers for Subumbra."""

from __future__ import annotations

from subumbra_keys import (
    ADAPTER_ID_RE,
    ADAPTER_SCOPE_VARS,
    Any,
    AutomationInputError,
    BANNER,
    BootstrapFlowError,
    BUILTIN_ADAPTER_IDS,
    BUILTIN_TOKEN_SUFFIXES,
    CATALOG_DIR,
    CATALOG_JSON_FILE,
    CATALOG_RELEASE_PUBKEY_HEX,
    CATALOG_SIG_FILE,
    CF_RESOURCES_FILE,
    DATA_DIR,
    HOST_ENV_FILE,
    KEYS_FILE,
    KEY_ID_RE,
    KV_CONFIG_FILE,
    MANIFEST_FILE,
    PUBLIC_KEY_FILE,
    Path,
    RUNTIME_ENV_OUT,
    SESSIONS_DB_FILE,
    STRUCTURED_KV_SCHEMA_VERSION,
    SYSTEM_INTEGRITY_FILE,
    USER_TEMPLATES_DIR,
    WORKER_SRC,
    _ADAPTER_CATALOG_CACHE,
    _CATALOG_CACHE,
    _WIZARD_SECRETS,
    die,
    info,
    ok,
    step,
    warn,
    _automation_fail,
    _build_custom_adapter_scope_vars,
    _build_host_env_updates,
    _build_runtime_env_lines,
    _chown_to_subumbra,
    _choose_bootstrap_mode,
    _delete_file_if_present,
    _effective_manifest_adapters,
    _expand_template_into_policy,
    _format_adapter_line,
    _has_cf_credentials,
    _has_env_credentials,
    _infer_cf_worker_name_from_worker_url,
    _key_id_env_var_name,
    _load_adapter_catalog,
    _load_and_verify_catalog,
    _load_keys_payload_if_present,
    _load_manifest_key_ids_only,
    _load_manifest_records,
    _load_manifest_repair_authority,
    _load_policy_index,
    _load_policy_path_from_env,
    _load_public_key_from_pem,
    _load_unique_key_flags,
    _manifest_die,
    _next_generated_key_id,
    _normalize_adapter_id,
    _normalize_manifest_record,
    _normalize_policy_doc,
    _parse_adapter_ids,
    _parse_allowed_keys_csv,
    _parse_bool_flag,
    _parse_env_file,
    _parse_key_adapter_ids,
    _policy_adapter_ids,
    _policy_die,
    _policy_require_string,
    _prompt_after_automation_error,
    _prompt_allowed_keys,
    _prompt_app_label,
    _prompt_declared_adapter_ids,
    _prompt_hidden_line,
    _prompt_key_adapter_ids,
    _read_env_file_value,
    _read_runtime_credential_value,
    _representative_key_id_for_vault_instance,
    _resolve_env_key_id,
    _resolve_manifest_secret,
    _resolve_policy_for_key,
    _rewrite_manifest_adapters_line,
    _secure_data_dir,
    _sync_host_env_file,
    _unique_key_env_var_name,
    _upsert_env_file,
    _validate_allowed_keys,
    _validate_safe_pattern,
    _vault_instance_for_key,
    _write_keys_payload,
    _write_public_key_file,
    _write_runtime_env_file,
    _write_system_integrity,
    compute_policy_hash,
    json,
    os,
    yaml,
    re,
    secrets,
    shutil,
    sqlite3,
    subprocess,
    sys,
    tempfile,
    termios,
    tty,
    textwrap,
    time,
    urllib,
    datetime,
    timedelta,
    timezone,
    BytesParser,
    policy,
    b64encode,
    gc,
    hashlib,
    serialization,
    hashes,
    padding,
    Ed25519PublicKey,
    Iterable,
    NoReturn,
)
